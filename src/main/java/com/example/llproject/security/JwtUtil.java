/*
package com.example.llproject.security;

import io.jsonwebtoken.Claims;
import io.jsonwebtoken.Jwts;
import io.jsonwebtoken.SignatureAlgorithm;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.stereotype.Component;

import java.util.Date;
import java.util.function.Function;

  @Component
  public class JwtUtil {

    @Value("${jwt.secret}")
    private String jwtSecret;

    @Value("${jwt.expiration}")
    private long jwtExpiration;

    public String generateToken(String username) {
      // ... Add code to generate a JWT token using the username as the subject
    }

    public boolean validateToken(String token) {
      // ... Add code to validate the JWT token
    }

    public String getUsernameFromToken(String token) {
      // ... Add code to extract the username (subject) from the JWT token
    }

    // ... Add other utility methods as needed
  }


*/
