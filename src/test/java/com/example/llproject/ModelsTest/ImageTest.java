/*
package com.example.llproject.ModelsTest;

import com.example.llproject.model.Image;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertEquals;

class ImageTest {

  @Test
  void testImageConstructor() {
    // Create an Image object using the constructor
    Image image = new Image(100, "image.jpg", "path/to/image.jpg");

    // Verify the image attributes
    assertEquals(100, image.getImageId());
    assertEquals("image.jpg", image.getImageName());
    assertEquals("path/to/image.jpg", image.getImageUrl());
  }

  @Test
  void testImageGetterSetter() {
    // Create an Image object
    Image image = new Image();

    // Set the image attributes using the setter methods
    image.setImageId(100);
    image.setImageName("image.jpg");
    image.setImageUrl("path/to/image.jpg");

    // Verify the image attributes using the getter methods
    assertEquals(100, image.getImageId());
    assertEquals("image.jpg", image.getImageName());
    assertEquals("path/to/image.jpg", image.getImageUrl());
  }

  @Test
  void testImageToString() {
    // Create an Image object
    Image image = new Image(100, "image.jpg", "path/to/image.jpg");

    // Verify the toString representation of the image
    assertEquals("Image{imageId=100, imageName=image.jpg, imageUrl=path/to/image.jpg}", image.toString());
  }

}
*/
